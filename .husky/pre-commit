echo "pre-commit hook invoked"
BRANCH=$(git rev-parse --abbrev-ref HEAD)
if [ "$BRANCH" = "master" ]; then
    echo "当前在master分支新增commit, 如果需要发包，请修改版本号"
    exit 1
else
    echo "Not on main branch, skipping npm publish."
fi
